from setuptools import setup, find_packages

setup(
      name='work_stream',
      version='0.0.1',
      author='fengqing',
      description='工作流',
      packages=find_packages(),
      zip_safe=True,
)
